import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { PrimaryBtnWithIcon, SecondaryBtnWithIcon } from "./index";
import { withKnobs, boolean } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { Cart } from '../../assets/icons/index';

<Meta title="Design System|Buttons With Icons" component={PrimaryBtnWithIcon} />

# Buttons With Icons

Renders a Button with an Icon. Combine the Button Component and Icon Component.

## Usage

For Buttons that have Icons inside that denote some purpose.

## Primary Buttons With Icons

Styled with the Primary Color with the Icon on the left and button text on the right

<Preview withToolbar>
  <Story name="primary-btn-with-icon">
    <PrimaryBtnWithIcon 
      onClick={action('button-click')} 
      disabled={boolean('Disabled', false)}
      buttonText="Add to Cart"
      Icon={Cart}
    >
      Hello world
    </PrimaryBtnWithIcon>
  </Story>
</Preview>

## Secondary Buttons With Icons

Styled with the Secondary Color with the Icon on the left and button text on the right

<Preview withToolbar>
  <Story name="secondary-btn-with-icon">
    <SecondaryBtnWithIcon 
      buttonText="Add to Cart"
      Icon={Cart}
    >
      Hello world
    </SecondaryBtnWithIcon>
  </Story>
</Preview>


